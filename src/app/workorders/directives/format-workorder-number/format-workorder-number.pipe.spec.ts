import { FormatWorkorderNumberPipe } from './format-workorder-number.pipe';

describe('FormatWorkorderNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWorkorderNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
