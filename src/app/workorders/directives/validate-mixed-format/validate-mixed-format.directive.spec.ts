import { ValidateMixedFormatDirective } from './validate-mixed-format.directive';

describe('ValidateMixedFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateMixedFormatDirective();
    expect(directive).toBeTruthy();
  });
});
