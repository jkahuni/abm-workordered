import { MachinesFilterPipe } from './machines-filter.pipe';

describe('MachinesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MachinesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
