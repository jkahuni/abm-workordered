import { ValidateSentenceFormatDirective } from './validate-sentence-format.directive';

describe('ValidateSentenceFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateSentenceFormatDirective();
    expect(directive).toBeTruthy();
  });
});
