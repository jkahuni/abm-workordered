import { ValidateNameFormatDirective } from './validate-name-format.directive';

describe('ValidateNameFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateNameFormatDirective();
    expect(directive).toBeTruthy();
  });
});
