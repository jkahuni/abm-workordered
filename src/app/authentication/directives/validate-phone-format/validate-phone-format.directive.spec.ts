import { ValidatePhoneFormatDirective } from './validate-phone-format.directive';

describe('ValidatePhoneFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePhoneFormatDirective();
    expect(directive).toBeTruthy();
  });
});
